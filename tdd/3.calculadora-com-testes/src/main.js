const sum = (x, y) => x + y
const division = (x, y) => y !== 0 ? x / y : 'error'
const subtraction = (x, y) => x - y
const multiplication = (x, y) => x * y

export { sum, division, subtraction, multiplication }
